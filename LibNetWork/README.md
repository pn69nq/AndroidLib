# 基础
1、lambda
```
    (type1 arg1,type2 arg2.....)->{}
    参数类型可以省略,当参数只有一个的时候()可以省略，当表达式中只有一句的时候{}可以省略
    
```
    
2、





```
    HttpManager->doHttpDeal(BaseApi baseApi)
    {
        //1、创建retrofit
        //1.1 实例化okhttp(已经创建可以直接使用）
        //1.2 实例化retrofit(已经创建可以直接使用）
        
        
        //2、通过baseApi实现类获得Observable(调用对应的http请求),需要传染retrofit参数
        
        //3、关联对应的观察者()
        
        //4、生命周期的解决？
    }

```
